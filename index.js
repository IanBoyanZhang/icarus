/*var fs = require('fs');

var path = [].push('.');

var srcPaths = path;

// map -> iterator
Promise.all(srcPaths.map((srcPath) => {
  return fs.stat(srcPath)
}));*/

var esprima = require('esprima');
